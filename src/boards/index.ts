import PerspectiveBoard from "./PerspectiveBoard.vue";
import PlaneBoard from "./PlaneBoard.vue";
export { PerspectiveBoard, PlaneBoard };

import type { Reference } from "./models/Reference.model";
import type {
  ArrowGeometry,
  CircleGeometry,
  RectGeometry,
  Colors,
} from "./models/geometry.model";
export type { Reference, ArrowGeometry, CircleGeometry, RectGeometry, Colors };
